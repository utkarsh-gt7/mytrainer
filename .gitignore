# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage

# Local env files — never commit personal machine values or real secrets here.
# NOTE: committed env files are allowed and intentional:
#   - .env.example        → template
#   - .env.production     → build-time public Firebase web config (safe to commit)
.env
.env.local
.env.development.local
.env.production.local
.env.test.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
